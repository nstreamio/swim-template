apply plugin: 'application'

version = '0.0.1'
mainClassName = 'example.ExamplePlane'
ext.moduleName = 'example'

repositories {
    mavenCentral()
}

dependencies {
    implementation group: 'org.swimos', name: 'swim-server', version: '4.0.1'
}